# Futures Bot (Binance USD-M, Long/Short, No Spot)
See .env.example and run:
```
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env && python run.py
```
