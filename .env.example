USE_GCP_SECRETS=true
GCP_PROJECT=futures-bot-469715
GCP_SECRET_PREFIX=futures-bot

# Toggle to select secrets automatically
USE_TESTNET=true

# Exchange and runtime
EXCHANGE=binanceusdm
DRY_RUN=true
MARGIN_MODE=isolated
LEVERAGE=3

# Universe / capacity (global)
UNIVERSE_SIZE=200
MAX_POSITIONS=3

# Ops cadence
POLL_SECONDS=30
MONITOR_SECONDS=10
PNL_MONITOR_SECONDS=2
ORPHAN_MONITOR_SECONDS=2
ORPHAN_PROTECT_SECONDS=45
ORPHAN_MIN_AGE_SECONDS=60
UNIVERSE_MONITOR_SECONDS=2
SCAN_WHEN_FLAT_SECONDS=10

# Risk (global caps)
ACCOUNT_EQUITY_USDT=100
RISK_PER_TRADE=0.01
ABS_RISK_USDT=0
MAX_NOTIONAL_FRACTION=0.30
MIN_NOTIONAL_USDT=10
MARGIN_BUFFER_FRAC=0.90

# Strategies to load (comma-separated)
STRATEGIES=mtf_ema_rsi_adx,breakout
TARGET_SPLITS=0.5,0.3,0.2

# Logging / timezone
LOG_TRADES_CSV=trades_futures.csv
TIMEZONE=indian

# --- Per-strategy overrides (optional) ---
# Prefix: STRAT_<ID>_
# mtf_ema_rsi_adx
# STRAT_MTF_EMA_RSI_ADX_TIMEFRAME=15m
# STRAT_MTF_EMA_RSI_ADX_HTF_TIMEFRAME=1h
# STRAT_MTF_EMA_RSI_ADX_RSI_LONG_MIN=52
# STRAT_MTF_EMA_RSI_ADX_RSI_SHORT_MAX=48
# STRAT_MTF_EMA_RSI_ADX_MIN_ADX=18
# breakout
# STRAT_BREAKOUT_TIMEFRAME=5m
# STRAT_BREAKOUT_HTF_TIMEFRAME=1h
# STRAT_BREAKOUT_RSI_LONG_MIN=52
# STRAT_BREAKOUT_RSI_SHORT_MAX=48
# STRAT_BREAKOUT_MIN_ADX=18
# STRAT_BREAKOUT_VOL_MULT=1.5
